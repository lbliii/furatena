---
title: Get Started — Read-through
description: One continuous reading surface stitched from the Get Started catalog nodes.
layout: collection
collection: get-started
weight: 2
---

This **collection view** loads ordered nodes from the live catalog graph and
renders them inline — no build step, no duplicated markdown.

Use `layout: collection` and `collection: <id>` in front matter. Define the node
order in `data/collections.yaml`.
