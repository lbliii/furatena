---
title: Chirp 0.1.1
description: CLI, filesystem routing, reactive blocks, SSE safety, form macros, production support
type: changelog
date: 2026-02-15
draft: false
lang: en
tags: [release, changelog, cli, routing, sse, forms, production]
keywords: [release, 0.1.1, chirp new, chirp run, filesystem routing, reactive blocks]
category: changelog
---

# Chirp 0.1.1

**Key additions:** CLI scaffolding, filesystem-based page routing, reactive block pipeline, SSE safety checks, form macros, and production deployment support.

---

## Highlights

### CLI

- `chirp new <name> [--minimal]` — Scaffold a project with app, templates, static assets, and tests
- `chirp run <app>` — Start the dev server from an import string (e.g. `chirp run myapp:app`)
- `chirp check <app>` — Validate hypermedia contracts from the command line

### Routing & Pages

- **Filesystem-based page routing** — Discover pages from directory structure with layout nesting
- **`app.provide()`** — Dependency injection for request-scoped context providers
- **Safe target** — `hx-target` safety for event-driven htmx elements

### Templates & Forms

- **`Template.inline()`** — Prototyping shortcut for string-based template rendering
- **Reactive block pipeline** — Structured reactive templates with derived state
- **`field_errors`**, **`qs`** — Built-in filters; **`form_or_errors()`**, **`form_values()`** — Form helpers
- **Form macros** — `chirp/forms.html` with `text_field`, `textarea_field`, `select_field`, `checkbox_field`, `hidden_field`

### SSE & Production

- **SSE safety checks** — Contract validation for event streams and event cross-reference
- **Pounce Phase 5 & 6** — Production server feature support
- **Security headers middleware** — X-Frame-Options, X-Content-Type-Options, Referrer-Policy
- **View transitions** — OOB swap support for View Transitions API
- **Deployment docs** — Production deployment guide

### Examples

- **Pokedex** — SQLite + migrations + full CRUD
- **Chat** — Real-time chat with SSE
- **Production** — Thank-you flow with production patterns
- **Accessibility** — ARIA patterns and keyboard nav
- **API** — JSON API example
- **RAG demo** — Enhanced with share URLs, sync script, improved UI

---

## Breaking Changes

None. Fully backward-compatible release.

---

## Upgrading

:::{tab-set}
:::{tab-item} UV

```bash
uv pip install --upgrade bengal-chirp
```

:::

:::{tab-item} pip

```bash
pip install --upgrade bengal-chirp
```

:::

:::{/tab-set}

With chirp-ui:

```bash
pip install bengal-chirp[ui]
```
