---
title: Chirp 0.1.5
description: chirp-ui filter fix — html_attrs, use_chirp_ui auto-registers
type: changelog
date: 2026-03-04
draft: false
lang: en
tags: [release, changelog, chirp-ui, templates]
keywords: [release, 0.1.5, chirp-ui, html_attrs, filters]
category: changelog
---

# Chirp 0.1.5

**Key additions:** Fixes `TemplateSyntaxError: Unknown filter 'html_attrs'` when using chirp-ui templates. `use_chirp_ui(app)` now auto-registers chirp-ui filters.

---

## Fixed

- **chirp-ui filters** — `use_chirp_ui(app)` now auto-registers chirp-ui filters (`html_attrs`, `bem`, `field_errors`, `validate_variant`). `create_environment` adds env-level fallback when chirp-ui is installed, so filters are present even without explicit `register_filters`.

---

## Breaking Changes

None. Fully backward-compatible release.

---

## Upgrading

:::{tab-set}
:::{tab-item} UV

```bash
uv pip install --upgrade bengal-chirp
```

:::

:::{tab-item} pip

```bash
pip install --upgrade bengal-chirp
```

:::

:::{/tab-set}

With chirp-ui:

```bash
pip install bengal-chirp[ui]
```
