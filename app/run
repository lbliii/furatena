#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

cd "$ROOT"
export CHIRP_SKIP_CONTRACT_CHECKS="${CHIRP_SKIP_CONTRACT_CHECKS:-1}"

exec uv run fura serve "$@"
