"""Furatena docs-core — bundled ``.chirp-theme-*`` CSS shipped with Furatena."""

from __future__ import annotations

from pathlib import Path

ROOT = Path(__file__).resolve().parent
ASSETS = ROOT / "assets"
