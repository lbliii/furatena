Rollback, replacement, and deprecation image targets now require exact stable-release ownership, with non-revocation, registry-availability, and provenance checks wherever the operation requires a deployable image.
