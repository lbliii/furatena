Public projection inspection now reports a page as added to (or removed from) the sitemap and llms.txt surfaces. Those surfaces are published as an index that points to per-mount detail files, and the inspector previously read only the index, so it under-reported publication impact as `unchanged`.
